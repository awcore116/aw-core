# aw-core
aw-core is the open methodology behind AW — deterministic, auditable judgment models (no LLM in the scoring path) for structured decision-making. Includes two full reference implementations (Ledger, Flow) plus the shared signal-extraction logic they run on.
